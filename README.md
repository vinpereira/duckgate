# duckgate

Interactive SQL shell over S3 data (Parquet, Iceberg, CSV), powered by DuckDB, with table
discovery from AWS Glue and a local TOML config for environment overrides.

> Work in progress — full usage docs land as the CLI commands are implemented.
